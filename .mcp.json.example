{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@bytebase/dbhub@latest",
        "--transport=stdio",
        "--config=./dbhub.toml"
      ]
    }
  }
}
