<!--
Usage: copy this file to `docs/adr/NNNN-<short-slug>.md` where `NNNN` is the
next free zero-padded number (look at the highest existing file). Replace
the body sections; keep the four headings (`Status`, `Context`, `Decision`,
`Consequences`) and the section order. Update Status only — never edit a
merged ADR's substance; supersede it with a new ADR whose Decision section
references this one.
-->

# ADR-NNNN — <short noun phrase>

## Status

Proposed | Accepted | Deprecated | Superseded by ADR-XXXX

## Context

The forces at play and the problem being addressed. Descriptive, not
normative — capture the situation, not the resolution. Include the
constraints that make the decision non-obvious.

## Decision

The change being made, in active voice: "We will …". One paragraph or a
short list. Avoid hedging; the ADR exists because a choice was made.

## Consequences

What becomes easier and what becomes harder once this decision is in
effect. Cover positive, negative, and neutral outcomes. Include any
follow-on work the decision implies but does not itself do.
